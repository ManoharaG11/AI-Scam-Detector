const axios = require('axios');

const prompt = ''r'zaSyDbIwvXRpaLdr-k2jD4qgstiQ1e040Fs'
prmpt'Sy hel iord'
const models = [
consi-flash5-pro',
   ni-1.0-pro',
   ni-pro-latestp
   ni-1.5-flas0-p
   ni-1.5-prproaes
];ud
   onst respon5e = -l t lolis.com/v1beta/models/${model}:generateContent?key=${API_KEY}`,
            { contents: [{ parts: [{ text: prompt }] }] },
            { timeout: 15000 }
        );del
          const text = response.data.candidates[0].content.parts[0].text;
            cosesponsele.await axios.post(
            log(`✅ ${model}: WORKS! Response: ${text.tr(1b)taodl,
    ee      const msg = err.response?.datp;mp },
              return fal5se;
        }
}    ttxt = eponse.data.canddates[0].ctet.prts[0].txt
    `✅ ${model}: WORKS! ${xm}`
    async function main() {
      console.log('Testing available Gemini models...\n');
        for temttus =s;
        const mg=es
`❌$d}${tu}bsg0100trfl     availablemodelmdel l)}mn